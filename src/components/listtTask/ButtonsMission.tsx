import React from 'react'

interface Prop {
    status: string
}

export default function ButtonsMission() {
  return (
    <div>
        <button>delete</button>
        <button>Progress</button>
    </div>
  )
}
